import { AB_GYM_LOGO_BASE64 } from '../lib/logoBase64';

// Export direct base64 data-uri so browsers never load a stale cached asset or suffer MIME type mismatch
export const abGymLogo = AB_GYM_LOGO_BASE64;
export default abGymLogo;
